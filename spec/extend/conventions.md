# Conventions

- **Language (no exceptions):** All documentation, comments, commit messages, user-facing CLI strings, and any other project prose must be written **only in English**. This includes everything under `spec/`, `docs/`, code comments, docstrings, `README`/`AGENTS`/`RULE` files, and packaging or script help text.
