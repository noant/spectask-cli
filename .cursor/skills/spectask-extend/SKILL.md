---
name: spectask-extend
description: Use when adding a new rule file under spec/extend/ (per spec/main.md).
---

Read `spec/navigation.md`, then `spec/main.md`; add `spec/extend/{name}.md`, register it in the `spec/extend/` list in `spec/navigation.md`, and obey Folder Structure and allowlist rules in `main.md`.
